\chapter{\textbf{Act III:} Manifold Geometry Sets Computational Capacity}
\label{chapter:act-III}

\section{What is being asked, and along which axis}
\label{sec:act3-frame}
\bigskip
% Section 6.1 (CROSS_ACT_SPINE.md, Act III, seven-section breakdown of 28 Aug).
% Carries NO claim and points at no figure. Sources: fact sheet 13 throughout.
% FORBIDDEN here (sheet 13): any result; "the two axes" for sigma against f;
% "the biological cut"; introducing f before P5's single sentence; re-deriving
% the two-axis argument; "89% an artifact of nominal-sigma matching"; "the
% connectome is subcritically worse than ER"; any margin without its levels;
% "compact bulk"; "the connectome is a better reservoir".
%
% PROVISIONAL LABELS used below, to be renamed once the sections exist:
%   sec:act3-memory-sigma (6.2)  sec:act3-prediction-sigma (6.3)
%   sec:act3-lesion-memory (6.4) sec:act3-lesion-generation (6.5)
%   sec:act3-oneaxis (6.6)       sec:act3-scope (6.7)
%   fig:two-axes = chapter 3's F3.

% P1: what arrives, and the boundary that arrives with it.
Three things arrive from Chapter~\ref{chapter:act-II}, and none of them is a
statement about what a network can compute. The first is the \textbf{common
mode}, the pattern in which every unit moves together: the previous chapter
established that a non-negative substrate cannot avoid carrying one, and
measured how far each substrate is dominated by its own
(Section~\ref{sec:act2-perron}). It arrives as a description of what the states
look like, not as an explanation of anything they do. The second is the \textbf{ridge
effective rank} $d_{\mathrm{eff}}$ (Equation~\ref{eq:deff}), which counts the
directions of the state matrix a linear readout can act on. It arrives earned
rather than assumed, and with its scope attached: it is the right quantity for
asking how much of a manifold a readout can use and the wrong one for asking
what shape that manifold has (Section~\ref{sec:act2-counting}). The third is the
reason the second must be read more than once. Where a substrate's Gram spectrum
sits relative to the readout's regularisation floor is not fixed by the
substrate but moves with how hard the network is driven
(Section~\ref{sec:act2-floor}), so \textbf{usable dimensionality is a property of a
substrate at an operating point}. A fourth thing arrives and it is a
boundary rather than a result. On non-negative weights the shape of the
trajectory does not move across the operating-point sweep while predictive
performance falls sharply, so capacity is lost there with the geometry intact,
and what governs that loss was left open and named as open
(Section~\ref{sec:act2-temporal}). This chapter does not narrow itself around
that boundary; Section~\ref{sec:act3-prediction-sigma} is where it is measured.

% P2: the two demands, and the third that joins them.
What a substrate is asked for is not one quantity. A readout can be asked to
reconstruct an input the network has already seen, which requires that traces of
the past still be separable in the present state; or it can be asked to
sustain a signal on its own, driving itself from its own output with no further
input to correct it. The first is a demand on how many independent directions
survive; the second is a demand on whether the dynamics stay in a regime where a
trajectory can be continued at all. This chapter puts both to the same
substrates, along the same axis, and its unifying question is whether the two
answers are two facts or one. Which task carries which demand was fixed in
advance, before any of the data below was inspected, so that no claim can be
rescued by the observation that there is always some task on which it holds.
Retention is carried by the \textbf{memory-capacity} task, with NARMA-10 and
Mackey-Glass as corroboration. Generation is carried by the \textbf{Lorenz prediction}
task, and it has \emph{no} corroborating task: the regime switch and the
resistance margin reported below rest on Lorenz alone. The joint question is
carried by the two together, and what that costs is returned to in
Section~\ref{sec:act3-scope}.

% P3: the primary variable, stated concretely.
Both demands are made along one variable. Every substrate in this thesis is run
as the operator $\sigma W/|\lambda_1|$, whose spectral radius is exactly $\sigma$
for every variant by construction and whose bulk radius is
$\sigma \cdot \mathrm{bulk95}$. That $\sigma$ is the \textbf{operating point}:
how hard the recurrent dynamics are driven, and the one quantity in this
comparison that has a biological counterpart a brain actually moves, through
neuromodulation, arousal and plasticity. It is the primary variable of this
thesis and every headline claim in this chapter is made along it. The regimes
the comparison is run across, subcritical, near-critical and supercritical, are
positions on that variable, and the supercritical window used throughout is the
shared one defined by the connectome's own critical scale, which means it is not
symmetric between substrates: at the top of it each null is running far past its
own critical scale while the connectome has only just reached its.


% P4: both matching axes, carried forward from chapter 3 and not re-derived.
% Sources: fact sheet 13, movement 3, rows 2, 3, 4. Numerals: 3.
% FORBIDDEN here (sheet 13): re-deriving the two-axis argument, which is
% chapter 3's, contribution 5's and F3's; "89% an artifact of nominal-sigma
% matching"; "the connectome is subcritically worse than ER"; the 1.7x ratio
% quoted without its levels; "the two axes" for sigma against f.
Two matching axes follow from that normalisation, and
Chapter~\ref{chapter:methods} establishes both
(Figure~\ref{fig:f3-two-axes}). Matching on nominal $\sigma$ holds the
spectral radius fixed and lets the bulk radius vary; matching on
$\sigma\!\cdot\!\mathrm{bulk95}$ holds the bulk radius fixed and leaves the
Perron root deliberately unmatched. Neither choice is neutral, and the second
is the less neutral of the two in this chapter: the mechanism under test is
the Perron mode, and at the matched point the connectome is run at
$\sigma = 6.0$ against Erdős--Rényi's % TIER0 1.1
$3.54$, % TIER0 1.1
a Perron root $1.7$ times larger, % TIER0 1.1
so that axis hands the connectome more of the very quantity proposed as the
cause. The subcritical deficit those two axes disagree about is present at
matched spectral radius and absent at matched bulk radius, which is why every
result below is reported on both and rests on surviving both.

% P5: the one f sentence, and the operating-point guard.
% Sources: fact sheet 13, movement 3 (last row) and movement 4 (rows 3 and 6).
% The orientation table is deliberately not built; its one load-bearing
% sentence is carried here instead. Requires a matching edit to
% CROSS_ACT_SPINE.md, which commits section 6.1 to the table.
% FORBIDDEN here (sheet 13): "the biological cut" for f = 0; introducing f as a
% second axis of the chapter or before section 6.4, beyond this one sentence;
% "the two axes" for sigma against f; any result.
Two scope statements close this section. The first is that
Sections~\ref{sec:act3-memory-sigma} and~\ref{sec:act3-prediction-sigma} are
measured entirely on non-negative weights, and that a manipulation is introduced
in Section~\ref{sec:act3-lesion-memory}: the sign fraction $f$, the proportion of
weights made negative. It enters as an intervention and not as a second axis of
the chapter. Macroscale diffusion tractography cannot represent the sign of a
connection, so $f = 0$ is what the instrument produces rather than a property of
cortex, and grading $f$ upward asks what the missing half of the biology would
do. The second concerns how the results below are to be read against one
another. They are measured at operating points that differ by more than a
factor of three, and the operating point is not a detail of the setup but the
variable the chapter is about: the connectome's resistance to collapse appears
far above its own critical scale, at $\sigma \approx 7.6$ to $8.0$, % TIER0 2.3
while the generative advantage over the nulls is measured near criticality, at
$\sigma = 2$. % TIER0 2.6
Quoted without its operating point, either statement makes the other look false.
Every claim below therefore names the operating point it is measured at and the
axis it is matched on, and none should be read without them.





\section{Memory along the regime axis}
\label{sec:act3-memory-sigma}
\bigskip
% Section 6.2 (CROSS_ACT_SPINE.md, Act III, seven-section breakdown of 28 Aug).
% Claims: A3M.1, A3M.2, A3M.3, A3M.4, A3M.5, A3M.7, A3M.8 (act3a_memory.md 1).
% Figures: F7 (lead, and a workshop figure), F10, F9, F11 panel (a) at its f = 0
% column only. Sources: fact sheet 14 throughout, with three corrections from the
% audits of 29 Aug recorded in GAPS.md B23 to B25.
% Numerals: 22. Over the 8-16 budget because the spine merges three results with
% three figures into one section. First two cuts if it must come down: the three
% null peak d_eff values are already absent (F7's caption carries them), so cut
% (i) the +0.93 ordering value in P2, leaving the regional qualifier in words,
% and (ii) the 89.0 / 11.4 pair in P5, leaving a back-reference to chapter 5.
% ENTIRELY AT f = 0. The single permitted mention of f is the last clause of P6.
% FORBIDDEN here (sheet 14): "always worst"; any peak-capacity claim in either
% direction; "the connectome is a better reservoir"; "the connectome is
% subcritically worse than ER"; "scale-invariant" full stop; "bulk95 is the
% ladder controller"; the pooled supercritical correlation contrast (+0.796
% against -0.004), which TIER0 3.7 says must not be quoted at all; any memory
% statement without its sigma; a curvature or trajectory-geometry account of
% memory; "compact bulk"; nobody-degrades, the nulls catching up and
% hub-targeted inhibition, all of which are 6.4's.
%
% AUDIT NOTES, 29 Aug, from the two read-only checks:
%  - P4 uses 12.32 / 2.80 with 4.40 to 4.42. These are the same run. The pair
%    12.28 / 2.82 is the same quantity before the ridge reparameterisation
%    (GAPS B14, resolved). CONVENTIONS' standing pairing crosses the two and is
%    deliberately not followed.
%  - P4's aggregation clause says pooled median over supercritical cells, which
%    is what the artifact does. Sheet 14 says "seed medians"; that does not
%    return the published values (GAPS B25).
%  - P2 quotes only +0.93 from TIER0 3.2. The subcritical and near-peak values
%    failed their reproduction gate (GAPS B23) and the near-peak sign is
%    disputed by the generating artifact (GAPS B24). The 2.53 sign flip is
%    unverified and is not used.
%
% PROVISIONAL LABELS: fig:f7-crossing, fig:f9-scale, fig:f10-parity,
% fig:f11-mechanism. Citation key aceituno2020 is a placeholder.

% P1: why the obvious comparison is unavailable, and what is measurable instead.
% Claim A3M.2. Figure F7(b). Numerals: 0.
A reservoir's memory is usually reported as a single number, the best it
achieves anywhere along the operating-point axis, and that number cannot answer
the question this section asks. Measured as the ridge effective rank
(Equation~\ref{eq:deff}), every substrate on the ladder peaks within a few
percent of the hard $d_{\mathrm{eff}} = N$ ceiling, % TIER0 1.2
because at its own optimum each is using very nearly every direction available
to it; the larger network reported below does not escape that ceiling either.
Peak capacity is therefore not resolvable at this size, and no claim is made
about it in either direction. What is resolvable is what happens on either side
of the peak. A ceiling can clip curves but it cannot manufacture a crossing
between them, so a difference in how fast substrates fall away from their optima
is robust to finite size in a way a difference between the optima is not. This
section reads the decay region, and the result it finds there is a crossing.

% P2: the crossing. Claim A3M.1. Figure F7(a, c).
% Numerals: 7. The three null peak values are F7's caption's, not the body's:
% the ordering within the nulls is not a claim at any scale (sheet 14).
On the axis that holds the bulk radius fixed, all four substrates rise together,
all four reach the ceiling, and they separate only on the way down
(Figure~\ref{fig:F7}). The connectome's peak is the lowest of the four,
at $432.4$ of a possible $448$; % TIER0 1.2
its three nulls peak above it. But at the top of the four-variant overlap, at
$\sigma \cdot \mathrm{bulk95} = 2.599$, % TIER0 1.2
which is the largest matched radius at which every substrate and every seed
still has coverage, the connectome retains
$47\%$ % TIER0 1.2
of its own peak against the nulls'
$28$, $22$ and $11\%$: % TIER0 1.2
a four-fold spread in what survives, in the reverse of the order in which the
substrates peaked. The ordering the peak gives and the ordering the decay gives
are opposite, and that crossing, rather than either ordering alone, is the
finding. It is not a property of the axis that produces it: the same retention
ordering holds on the nominal axis, where the Perron root rather than the bulk
is what is held fixed (Figure~\ref{fig:F3}), % TIER0 1.2
so it survives the matching choice that hands the connectome the larger gap.
What it is not is a global advantage. The ladder ordering is regional: it runs
the other way below criticality, where the nominal axis leaves the bulk
unmatched, it is absent near the peak, and above criticality it is strong and
consistent at $+0.93$. % TIER0 3.2
Stated on the axis that holds the bulk fixed, where the subcritical deficit is
not present at all, the defensible summary is parity below criticality and
advantage above it.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{F7.png}
    \caption[Matched on the bulk radius, the connectome peaks lowest and retains
    most]{\textbf{Matched on the bulk radius, the connectome peaks lowest and
    retains most: the memory result is a crossing, not a capacity difference.}
    Memory-capacity task, $N = 448$, non-negative weights, ten seeds, ridge
    $\alpha = 10^{-6}$. \textbf{(a)} Ridge effective rank against
    $\sigma \cdot \mathrm{bulk95}$, the axis that holds the bulk radius fixed.
    $\mathrm{bulk95}$ is a per-seed quantity for the three resampling nulls, so each
    seed is reindexed onto a common grid before the median is taken; the dashed rule
    at $2.599$ marks the top of the range every substrate and every seed covers. All
    four rise together to the $d_{\mathrm{eff}} = N = 448$ ceiling and separate only
    on the way down. \textbf{(b)} At the peak they are not separable: each lands
    within $3.5\%$ of the ceiling, the connectome at $0.965$ against the nulls'
    $0.993$ to $0.997$. \textbf{(c)} The decay is separable and is the result: at the
    rule in (a) the connectome holds $47\%$ of its own peak against $28$, $22$ and
    $11\%$. A ceiling can clip curves but cannot manufacture a crossing.}
    \label{fig:F7}
\end{figure}

% P3: parity at the peak, with its interval, and the Aceituno reconciliation
% placed on the panel that reproduces their ordering. Claims A3M.3, A3M.8,
% A3M.7. Figure F10. Numerals: 6. The d_eff-to-MC correspondence value is
% deliberately absent: it has two aggregation units (+0.999 over n = 52 seed
% medians, +0.998 over 520 cells) and naming the unit in body prose costs a
% clause for a number that is not the point of the sentence. F10's caption
% carries it with its unit.
The peaks cannot be separated as capacities, but they can be compared as a
statement about their similarity, and that comparison is worth making precisely
because it runs against the chapter's direction. Paired within seed, each
substrate at its own optimum and each pair sharing input weights and input
series, the connectome's peak memory capacity sits
$2$ to $6\%$ % TIER0 3.4
below the nulls' (Figure~\ref{fig:F10}). The intervals are what make that
readable. Against Erdős--Rényi and against the weight-permuted placement control
the difference excludes zero at every ridge strength tested; against
degree-matching it does so at
one of five, % TIER0 3.4
and the point estimate changes sign at one of them. The defensible statement is
therefore \emph{parity} at the peak, not a deficit, and certainly not that this
substrate is the worst of the four. That the five ridge strengths span five
orders of magnitude makes the panel a robustness check rather than a search: the
correspondence between usable dimensionality and measured memory holds at every
one of them, provided the regularisation is raised in both places at once, so
the readout's regularisation is not what produces the result in either
direction. The same panel is where an apparent disagreement with the literature
resolves. At the smallest ridge, nearest the pseudoinverse limit that work
optimises, the reported spread-beats-compact ordering reproduces exactly on
these substrates, with Erdős--Rényi highest and the connectome
lowest~\cite{aceituno2020tailoring}. It is reproduced rather than contradicted, and the
two results are about different places on the axis: spread wins at the peak, and
a large spectral gap wins across the range.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{F10.png}
    \caption[At its own optimum the connectome's memory is at parity with the
    nulls]{\textbf{At its own optimum the connectome's memory is at parity with the
    nulls, not below them all.} Paired per-seed differences in peak memory capacity,
    each substrate taken at its own best spectral radius, ten seeds, with input
    weights and input series shared within a pair. Bars are $95\%$ $t$-intervals and
    filled markers are intervals excluding zero. \textbf{(a)} In memory-capacity
    units and \textbf{(b)} as a percentage of each null's own peak. The deficit is
    $2$ to $6\%$, and it excludes zero at all five ridge strengths against
    Erdős--Rényi and against the weight-permuted placement control, but at only one
    of five against degree-matching, where the estimate also changes sign: the open
    markers. The five strengths span five orders of magnitude and are shown because
    usable dimensionality tracks measured memory at $+0.999$ across all of them
    (seed-median statistic, $n = 52$), so the readout's regularisation is not what
    produces the result. At the smallest ridge, peak memory orders Erdős--Rényi, degree-matching,
    weight-permuted, connectome.}
    \label{fig:F10}
\end{figure}

% P4: does the decay difference survive a change of scale, and the one
% pre-registered test that did not settle. Claim A3M.4. Figure F9.
% Numerals: 9. Both margins are required (TIER0 2.4 says report both) and each
% travels with levels; the N = 1000 levels are in F9's caption rather than here,
% which is where the full value set belongs.
Whether any of this is an artifact of the parcellation is a question the
comparison has to answer rather than assume, so the memory sweep was repeated at
$N = 1000$ % TIER0 2.4
with the run length scaled to hold the ratio of retained timesteps to units
fixed and the ridge reparameterised to scale with the Gram matrix rather than
held at an absolute value. Two changes are therefore in play and they are
separated by a control at fixed $N$: the reparameterisation alone moves the
supercritical margin from
$4.35$ to $4.40$, % TIER0 2.4
so what happens beyond that is attributable to size (Figure~\ref{fig:F9}).
With the threshold set at the connectome's own critical scale and applied to
every variant, the supercritical median memory capacity is
$12.32$ against Erdős--Rényi's $2.80$ % TIER0 2.4
at the smaller size, a margin of $4.40$, and $4.42$ at the larger: % TIER0 2.4
flat across a change of scale. With each variant instead evaluated above its own
critical scale the same margin reads
$3.56$ and $3.85$, % TIER0 2.4
which is to say it grows by about a tenth. Both filters are defensible and they
say different things, so both are drawn, and what survives both is the claim that
the margin is not an artifact of the smaller network; what does not survive is
calling it scale-invariant without naming the filter. The first reading is
primary only because it evaluates every substrate over an identical range of
operating points, and it should be read knowing that this is also the filter
that flatters the connectome, since it samples each null further above its own
critical scale, where that null has already decayed further. One thing this run
was designed to settle and did not. It carried a pre-registered falsification
test, on the prediction that if the bulk radius is what orders the ladder then a
reversal in that quantity between the two sizes should reverse the memory
ordering with it. The memory ordering did not move. But the reversal the test
rests on is itself not significant at the larger size
($p = 0.16$), % TIER0 2.4
so the test was flawed rather than passed: the correct classification is
inconclusive, and it is inconclusive because the predictor was noisy and not
because the outcome was. Whether the bulk radius is what orders the ladder is
returned to in Section~\ref{sec:act3-lesion-memory}, where an intervention can
ask it more sharply than a change of scale could.

\begin{figure}[ht]
    \centering
    \includegraphics[width=\textwidth]{F9.png}
    \caption[The supercritical memory margin survives a 2.2-fold change in
    $N$]{\textbf{The supercritical memory margin survives across network scale, 
    under both definitions of ``supercritical''.} Memory-capacity task,
    non-negative weights, four substrates and ten seeds each; run length scaled from
    $3000$ to $6000$ to hold retained timesteps per unit near $5.5$, and the ridge
    reparameterised to scale with the Gram matrix rather than held at an absolute
    value. \textbf{(a)} Median memory capacity against nominal $\sigma$ for the
    connectome and Erdős--Rényi at both sizes; the dotted rules are the connectome's
    critical scale, $3.078$ at $N = 448$ and $3.985$ at $N = 1000$. \textbf{(b)} With
    that threshold applied to every variant, the connectome to Erdős--Rényi margin is
    $4.40$ and $4.42$. \textbf{(c)} With each variant taken above its own critical
    scale, $3.56$ and $3.85$. Both are drawn because the answer depends on the
    filter: the margin holds either way, but only (b) is flat. The larger network
    does not escape the $d_{\mathrm{eff}}$ ceiling.}
    \label{fig:F9}
\end{figure}

% P5: the mechanism, using chapter 5 and not re-establishing it. Claim A3M.5.
% Figure F11 panel (a) at its f = 0 column ONLY; the panel's own axis is 6.4's.
% Numerals: 5. The sigma-resolved rise is carried as a back-reference to
% chapter 5's F4b rather than as a four-number pair, because the panel that
% draws it is not in this chapter (GAPS flag 3).
What the gap buys is visible in the quantity the previous chapter measured. A
non-negative matrix has an all-positive leading eigenvector by
Perron--Frobenius, so every substrate here carries a common mode, and driving
the network harder drives that mode harder. The nulls give way to it. At the
supercritical operating point $\sigma = 6$ their common-mode amplitude stands at
$0.949$ to $0.989$, % TIER0 3.7
which is very nearly every unit pinned at the
same saturated value, doing the same thing at the same time. The connectome
reaches $0.759$, % TIER0 3.7
the least dominated of the four, and it does so while carrying by far the
largest Perron root of the four: the substrate with the most to be dominated by
is the one least dominated. The amplitude climbs with the operating point for
every substrate, and the connectome's climb starts lower and ends lower
(Chapter~\ref{chapter:act-II}, Figure~\ref{fig:F4}). That is what a
large spectral gap does here. It lets the leading mode be driven hard without
the bulk of the spectrum following it, so the activity that is not the common
mode survives, and Section~\ref{sec:act2-floor} is what connects that to the
measure this section reads: what stays clear of the readout's regularisation
floor is what the readout can use, and supercritically the connectome holds
$89.0\%$ of its directions more than a decade clear of that floor against
Erdős--Rényi's $11.4\%$. % TIER0 3.6
The decay difference in Figure~\ref{fig:F7} is that gap, read out.

% P6: the one-line version and both of its costs, stated in the same paragraph.
% Numerals: 0. The last clause hands 6.4 its motivation without naming f as a
% result or previewing any f > 0 number.
Put in one line, the finding is that the connectome does not make memory better.
Non-negativity makes memory worse for every substrate, and the connectome's
spectral gap makes it least worse. Both halves of that sentence carry a cost and
neither is left for a reader to find. The first is that at its own optimum this
substrate is not ahead, and the interval says so. The second is sharper: on the
quantity the reservoir literature has largely optimised, peak capacity at
minimal regularisation, this substrate loses, and the ordering that says so is
reproduced above rather than explained away. The reply is not that the
measurement is wrong but that the operating point is not a free parameter. A
tuned reservoir can be placed at its optimum and held there; a brain is driven
across a range by neuromodulation, arousal and plasticity, and what matters over
a range is how much is retained away from the optimum rather than how much is
available at it. That is the sense in which the advantage here is robustness and
not capacity. It also points at what would test the account rather than
illustrate it. Every substrate compared so far has a common mode, because every
one has non-negative weights, so comparison alone cannot show that the common
mode is what the difference is made of. The next section removes it.






\section{Prediction along the regime axis}
\label{sec:act3-prediction-sigma}
\bigskip
% Section 6.3 (CROSS_ACT_SPINE.md, Act III, seven-section breakdown of 28 Aug).
% Claims: A3P.4 and A3P.9 (act3b_prediction.md 1); RM 1 contribution 4's f = 0
% half and its scope limit. Sources: fact sheet 15 throughout.
% Figures: NONE. F13 is deliberately not floated here, so that the sign
% fraction is introduced only in 6.4; F13(c)'s counts are stated inline. The
% decay result has no figure in any case (SPINE open flag 2) and is carried by
% Table ~\ref{tab:act3-decay}, which is not a figure and needs no FIGURE_LIST
% slot.
% Numerals: 23. Over the 8-16 budget; the table absorbs about seven that would
% otherwise be prose. First cuts if it must come down: the rollout horizon pair
% in P1, leaving "the horizon does not bind" in words, and the memory levels in
% P4, leaving the inverted ordering in words.
% ENTIRELY AT f = 0. No f > 0 result appears, including the tight f > 0
% Jacobian comparison that would otherwise be P5's natural foil.
% FORBIDDEN here (sheet 15): any generative statement without its sigma; "50% of
% replicates"; "the generative advantage emerges with f"; "generation tracks
% trajectory straightness" or any graded curvature account; "capacity is gated
% by which dynamical regime the manifold is in" stated of f = 0, which is
% scope-limited to f > 0; "sigma_eff crosses 1 at the transition", falsified;
% "curvature is flat, so geometry does not matter"; offering a fourth
% explanation; any per-cell climate_error value; quoting the two lambda_min(J)
% aggregation units against each other.

% P1: what the closed loop asks that the driven tasks do not. Three sentences
% per the sheet; the protocol is the methods chapter's. Numerals: 3.
The two demands of this chapter are not scored the same way. A driven task is
corrected at every step, because the input the network receives is the true one,
so an error made at one step does not propagate into the state that is scored at
the next. A free-running network has no such correction: it is initialised on a
true trajectory, released, and thereafter driven by its own output, so errors
compound and what is scored is how long the generated trajectory stays with the
true one before leaving it. That duration, expressed in Lyapunov times so that it
is a property of the dynamics rather than of the integration step, is the valid
prediction time. One consequence of the arrangement must be stated plainly or
everything below is misread: trajectory curvature is measured under teacher
forcing, on the driven trajectory, while valid prediction time is measured on the
free run. This is not a defect, since the loss of generative capacity is a
property of the operator and is visible under drive, but the two quantities are
not measurements of the same trajectory. Neither is the instrument's range what
limits the result. The rollout horizon corresponds to
$16.3$ Lyapunov times % TIER0 2.6
and no cell anywhere in the sweep exceeded
$7.95$, % TIER0 2.6
so lengthening it would buy nothing; the floor, by contrast, is live, and at
$f = 0$ some
$7.2\%$ % A3P 2.6
of evaluations score exactly zero, which means every rollout window failed at the
first predicted step rather than drifting away gradually.

% P2: prediction decays with the operating point while the geometry does not
% move. SPINE open flag 2: no figure exists; stated inline, with the table
% carrying the range because flatness is the claim and two endpoints do not show
% it. Numerals: 6.
The previous chapter handed this section a boundary rather than a result: on
non-negative weights the shape of the driven trajectory does not move across the
operating-point sweep while predictive performance falls away, and what governs
that loss was left open. Table~\ref{tab:act3-decay} is that boundary measured.
The connectome's trajectory curvature holds at $0.26$~rad % TIER0 3.11
at every operating point sampled, from just above criticality to the top of the
sweep, while its valid prediction time falls by roughly a factor of ten. The fall
is not perfectly ordered, and the step at $\sigma = 8$ sits above the one at
$\sigma = 6$, % TIER0 3.11
so the ten-fold figure describes the range and not a monotone decline. What the
table also shows is that curvature is not simply insensitive. Erdős--Rényi's
curvature does move, at the very top of the sweep, where it rises to
$1.70$~rad; % TIER0 3.11
that is the collapse of the next paragraph appearing in the geometry column. The
connectome's does not move at all, and its capacity is lost anyway. Whatever is
being lost between $\sigma = 2$ and $\sigma = 11.2$, the shape of the manifold
does not record it.

\begin{table}[htbp]
\centering
\footnotesize
\caption[Prediction decays across the operating-point sweep while trajectory
curvature does not move]{\textbf{Prediction decays across the operating-point
sweep while the geometry does not move.} Lorenz task, $N = 448$, non-negative
weights, ten seeds, seed medians. Read \emph{across} a row: each row is one
quantity for one substrate over the sweep. The four rows are grouped by
substrate and are not ordered by anything, so nothing should be inferred from
reading down the leftmost column. Curvature is the mean angle between successive
trajectory segments, measured under teacher forcing; valid prediction time is
measured on the free run and is given in Lyapunov times. The connectome's
curvature is unchanged to two decimal places across a sweep over which its
prediction falls roughly ten-fold. Erdős--Rényi's curvature is unchanged until
the top of the sweep, where its trajectories collapse.}
\label{tab:act3-decay}
\begin{tabular}{@{}llccccc@{}}
\toprule
& & \multicolumn{5}{c}{nominal $\sigma$} \\
\cmidrule(l){3-7}
substrate & quantity & $2$ & $4$ & $6$ & $8$ & $11.2$ \\
\midrule
connectome    & curvature (rad)        & $0.26$ & $0.26$ & $0.26$ & $0.26$ & $0.26$ \\ % TIER0 3.11
              & valid prediction time  & $4.43$ & $2.81$ & $0.81$ & $1.18$ & $0.44$ \\ % TIER0 3.11
\addlinespace
Erdős--Rényi  & curvature (rad)        & $0.26$ & $0.26$ & $0.26$ & $0.27$ & $1.70$ \\ % TIER0 3.11
              & valid prediction time  & $3.73$ & $2.45$ & $1.18$ & $0.49$ & $0.23$ \\ % TIER0 3.11
\bottomrule
\end{tabular}
\end{table}

% P3: collapse resistance far supercritically, and the absence of any advantage
% near criticality, in the same breath with the radius named every time.
% Claim A3P.4. Numerals: 9. The f = 0.05 knife-edge count is deliberately
% omitted: it is the sheet's furthest step into f, and this section keeps the
% sign fraction out entirely.
Something does separate the substrates on this task, and it is found by asking
where each one fails rather than how well it does. Driven far above the
connectome's critical scale, at
$\sigma \approx 7.6$ to $8.0$, % TIER0 2.3
Erdős--Rényi's free-running trajectories stop being trajectories: five of its ten
seeds collapse onto a period-two orbit, generating a repeating two-cycle in place
of the attractor. None of the connectome's ten seeds does so anywhere in the
swept range (Fisher exact
$p = 0.033$). % TIER0 2.3
The connectome is the more resistant substrate, and the resistance is to a
qualitative failure rather than a quantitative one. That statement holds only
where it is made. Near criticality, at $\sigma = 2$, where every substrate is
close to its own best prediction, the same comparison at the same sign fraction
finds nothing: the paired margins against the three nulls are
$+0.28$, $-0.01$ and $+0.44$ % TIER0 2.6
Lyapunov times, and none of them is significant. Same substrates, same weights,
opposite answers, and the operating point is the entire difference between them,
which is why no generative claim in this thesis is quoted without the radius it
was measured at. Two limits travel with the collapse result. The independent
unit is the seed, of which there are ten per substrate, and repeated draws at
$f = 0$ are duplicates of one another rather than additional evidence. With ten
units the resolution of any test is bounded from below regardless of the effect,
so the claim rests on the size and the direction of the asymmetry, five against
zero, and the family of comparisons it belongs to is declared narrowly rather
than expanded after the fact.

% P4: three candidates, each ruled out on the data. Numerals: 6.
% sigma_eff is introduced here for the first time and is developed in 6.5. Its
% own maximum over the sweep (0.607) is cut; "two orders of magnitude below"
% carries it in words.
What sets generation at $f = 0$ is then a well-posed question with three obvious
answers, and the data rules out all three. The first is geometry, and
Table~\ref{tab:act3-decay} has already disposed of it: the quantity that would
have to register the loss does not move while the loss happens. The second is an
effective radius. A driven network's operator can be linearised about its own
state and assigned an effective spectral radius, $\sigma_{\mathrm{eff}}$, which
Section~\ref{sec:act3-lesion-generation} develops as the best empirical locator
of where generation breaks. At $f = 0$ it does not apply. Erdős--Rényi's
transition occurs at $\sigma_{\mathrm{eff}} = 0.014$, % TIER0 3.10
two orders of magnitude below the largest value it reaches anywhere in the sweep
and on the descending branch of its own curve, so nothing is being crossed; and
the connectome does not break at $f = 0$ at all within
$\sigma \leq 11.2$, % TIER0 3.11
so for that substrate there is no transition for a locator to locate. The third
is that memory is doing the work, generation being in some sense retention run
forward. The ordering is inverted. At $\sigma = 6$ the connectome holds a memory
capacity of $11.43$ against Erdős--Rényi's $2.42$, % TIER0 3.11
roughly four times as much, and a slightly \emph{lower} valid prediction time,
$0.81$ against $1.18$. % TIER0 3.11
The substrate with far more of the past available to it predicts the future
slightly less well.

% P5: the question left open and named as open. Claim A3P.9. Numerals: 1.
% Must not resolve by implication, and must not let a later mechanism read as
% general. F17 is deliberately not named.
No fourth candidate is offered here. Two further facts say why this is a
boundary of the present work rather than an omission from it. The first is that
the map governing the closed loop is not the recurrent matrix but that matrix
plus a rank-three term formed from the input and output weights, and the
evaluator used throughout this thesis does not expose the fitted readout, so the
operator that actually generates the trajectory was never available to be
measured. The second is that the failure is not a local linear bifurcation: the
smallest eigenvalue of the exact Jacobian, computed at the fixed point, never
approaches $-1$ % TIER0 3.11
anywhere in the sweep, so generation breaks while the fixed point it breaks away
from is still linearly stable. A mean-field correction was the obvious next
refinement and it is not the missing piece either. What remains is a capacity
that is lost with the geometry intact, on the only sign configuration the
instrument can produce, and no account of what governs it. The sections that
follow introduce an intervention and find a mechanism that operates under it;
that mechanism is not an answer to this question, and
Section~\ref{sec:act3-scope} states the limit explicitly rather than leaving it
to be inferred.








% \begin{figure}[ht]
%     \centering
%     \includegraphics[width=\textwidth]{F11.png}
%     \caption[Non-negativity makes memory worse for every substrate, and the
%     connectome's spectral gap makes it least worse]{\textbf{The connectome does not
%     make memory better; non-negativity makes it worse for every substrate, and the
%     connectome's spectral gap makes it least worse.} Memory-capacity task,
%     $N = 448$, ten seeds. \textbf{(a)} The common-mode amplitude, the grand mean of
%     the state matrix over time and units, absolute-valued per cell before the seed
%     median, against the fraction $f$ of weights made negative, at the supercritical
%     operating point $\sigma = 6$. With non-negative weights the nulls sit at $0.949$
%     to $0.989$, which is very nearly every unit pinned at one saturated value, while
%     the connectome sits at $0.759$ despite carrying much the largest Perron root.
%     Balancing the signs removes the common mode entirely, by two orders of magnitude
%     at $f = 0.5$, for every substrate. \textbf{(b)} The median absolute
%     connectome-minus-Erdős--Rényi memory gap across the swept range, under each
%     matching axis: matching on $\sigma \cdot \mathrm{bulk95}$ rather than on nominal
%     $\sigma$ absorbs only $26\%$ of the gap at $f = 0$ and leaves a residual near
%     zero once $f$ reaches $0.2$.}
%     \label{fig:F11}
% \end{figure}
